#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../scratch-l10n/scripts/build-i18n-src.js" "$@"
else 
  exec node  "$basedir/../scratch-l10n/scripts/build-i18n-src.js" "$@"
fi
